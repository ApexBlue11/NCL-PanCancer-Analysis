# Generated data — ~6 GB, fully reproducible via scripts/01_download.py
data/

# Run logs
*.log

# GSEA per-unit checkpoints (intermediate; T10 is the aggregated result)
results/gsea_parts/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Response letter is a private communication with the journal, not for publication
manuscript/response_to_reviewers.*

# Local working notes, not for publication
PROPOSED_CHANGES_LOCAL.md

# Publisher's copyedited manuscript and its backup. These carry the journal's
# internal editor queries as tracked comments, so like the response letter they
# stay local — this repository is public.
EJMO*.docx

# Journal-submission TIFFs: derived from the same code as the tracked PNG/PDF
# and ~6 MB per rebuild. Regenerate with `python scripts/10_figures.py`.
results/figures/*.tiff
